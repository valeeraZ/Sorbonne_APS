[
  PROC fill [xs:(vec int)]
    [
      VAR i int;
      SET i 0;
      VAR n int;
      SET n 0;
      WHILE (lt i (len xs))
      [
	      SET n (add i 38);
	      SET (nth xs i) n;
	      SET i (add i 1)
	    ];
      ECHO n
    ];
  CONST xs (vec int) (alloc 5);
  CALL fill xs
]
